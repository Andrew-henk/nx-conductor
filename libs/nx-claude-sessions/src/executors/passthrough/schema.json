{
  "$schema": "https://json-schema.org/draft-07/schema",
  "type": "object",
  "cli": "nx",
  "title": "Claude Code Passthrough",
  "description": "Direct passthrough to Claude Code with optional NX context",
  "properties": {
    "args": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Arguments to pass directly to Claude Code"
    },
    "library": {
      "type": "string",
      "description": "Optional library context to load"
    },
    "includeContext": {
      "type": "boolean",
      "description": "Include NX workspace context",
      "default": true
    },
    "dangerouslySkipPermissions": {
      "type": "boolean",
      "description": "⚠️  DANGEROUS: Skip all permission checks and safety validations",
      "default": false
    },
    "customClaudePath": {
      "type": "string",
      "description": "Custom path to Claude Code binary"
    },
    "workingDirectory": {
      "type": "string",
      "description": "Working directory for Claude Code execution"
    },
    "timeout": {
      "type": "string",
      "description": "Execution timeout",
      "default": "10m"
    },
    "interactive": {
      "type": "boolean",
      "description": "Run in interactive mode (inherit stdio)",
      "default": true
    }
  },
  "required": [],
  "additionalProperties": false
}